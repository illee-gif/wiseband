# 배포 체크리스트

이 사이트는 두 부분으로 이루어져 있어요.

- **프론트엔드** (`index.html`) — GitHub Pages로 호스팅하는 화면
- **백엔드** (`apps-script/Code.gs`) — 구글시트를 데이터 저장소로 쓰는 API

둘 다 구글/깃허브 로그인이 필요해서 이 부분은 직접 해주셔야 해요. 순서대로 따라오시면 됩니다.

## 1. 구글시트 + Apps Script 배포

1. 구글 드라이브에서 새 스프레드시트를 만듭니다. (이름 예: `이스린생활 DB`)
2. 메뉴에서 **확장 프로그램 → Apps Script** 를 엽니다.
3. 기본으로 열려있는 `Code.gs` 내용을 전부 지우고, 이 프로젝트의 [`apps-script/Code.gs`](apps-script/Code.gs) 내용을 그대로 붙여넣습니다.
4. 오른쪽 위 **배포 → 새 배포**를 클릭합니다.
   - 유형: **웹 앱**
   - 실행 계정: **나(본인)**
   - 액세스 권한이 있는 사용자: **모든 사용자**
5. 배포를 누르면 권한 승인 화면이 나옵니다 — 본인 계정으로 승인하면 됩니다.
6. 배포가 끝나면 나오는 **웹 앱 URL** (`.../exec`로 끝나는 주소)을 복사해둡니다.

> 시트에 `Data`라는 탭이 자동으로 생기고, 여기에 모든 회의록/연습일지/악보/영상/응원 글이 한 줄씩 쌓여요.

## 2. index.html에 API 주소 연결

`index.html`을 열어서 `<script>` 안의 이 줄을 찾습니다.

```js
var API_URL = ""; // Apps Script 웹 앱 배포 후 나오는 /exec URL을 여기에 붙여넣으세요.
```

1번에서 복사한 주소를 붙여넣으세요.

```js
var API_URL = "https://script.google.com/macros/s/여기에_긴_ID/exec";
```

## 3. 암호 바꾸기 (선택)

지금은 `1234`로 되어 있어요. 바꾸려면 **두 곳을 똑같이** 바꿔야 해요.

- `index.html` 의 `var PASSPHRASE = "1234";`
- `apps-script/Code.gs` 의 `var SHARED_KEY = '1234';` (바꾼 뒤 Apps Script에서 **배포 → 배포 관리 → 수정 → 새 버전으로 배포**까지 해줘야 반영돼요)

## 4. GitHub Pages로 올리기

1. GitHub에서 새 저장소를 만듭니다 (Private으로 만들어도 되지만, **Pages 사이트 자체는 공개 URL**이 됩니다 — 링크를 아는 사람만 접근 가능한 정도이고, 진짜 비공개는 아니에요. 그래서 암호 잠금을 같이 쓰는 거예요).
2. 이 폴더(`슬기로운이스린생활`) 내용을 그 저장소에 push 합니다.
3. 저장소 **Settings → Pages** 에서 Source를 `main` 브랜치 `/ (root)` 로 설정합니다.
4. 몇 분 뒤 `https://<깃허브계정>.github.io/<저장소이름>/` 주소로 접속하면 사이트가 열려요.

## 사용 방법 (멤버들에게 공유할 내용)

- 사이트 주소 + 암호를 넷이 공유합니다.
- 악보나 연습 녹음 파일은 공유 구글 드라이브 폴더에 먼저 올린 뒤, 그 파일의 **공유 링크**를 사이트의 해당 섹션 폼에 붙여넣습니다.
- 회의록/연습일지/응원 메시지는 사이트에서 바로 입력하면 구글시트에 쌓입니다.

## 문제가 생기면

- **저장은 되는데 다른 사람 화면엔 안 보여요** → 새로고침 해보세요. 지금은 저장 시 내 화면만 바로 갱신되고, 다른 사람은 페이지를 새로 열어야 최신 내용을 받아와요.
- **저장 눌러도 "저장 실패"가 떠요** → Apps Script 배포 단계에서 액세스 권한을 "모든 사용자"로 했는지 확인하세요. 그리고 브라우저 콘솔에 CORS 관련 에러가 있는지 확인해주세요.
